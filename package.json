{
  "name": "@design-tokens/root",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ftdigital/design-tokens.git"
  },
  "author": "FT Digital",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ftdigital/design-tokens/issues"
  },
  "homepage": "https://github.com/ftdigital/design-tokens#readme",
  "prettier": {
    "printWidth": 80,
    "semi": true,
    "singleQuote": false,
    "tabWidth": 2,
    "trailingComma": "none",
    "arrowParens": "avoid"
  },
  "husky": {
    "hooks": {
      "pre-commit": "eslint --fix"
    }
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:design-tokens": "yarn workspace @design-tokens/design-tokens build",
    "build:plugin-webpack": "yarn workspace @design-tokens/plugin-webpack build",
    "build": "yarn build:design-tokens && yarn build:plugin-webpack"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "prettier": "^2.8.7",
    "typescript": "^5.0.3"
  }
}
